# mongoose-js-demo
mongoose-js-demo
```bash
npm init -y
npm i mongoose

schema : structure of document, default values, validations, etc.

Model : Wrapper of mongoose schema. Mongoose schema is structure of document, default values, validations, etc.
model provides an interface to the database for creating, querying, updating, deleting records, etc.
```
